"""SVG legend builder.

Consolidates KaryoScope_draw_legend.py (light theme) and
KaryoScope-Telomere/.../make_legend.py (dark theme) into one builder
with a `theme` parameter.

Populated in Phase 3.
"""

# Phase 3 will fill this with:
#   - draw_legend(palette: dict, theme="light", columns=4, ...) -> drawsvg.Drawing
