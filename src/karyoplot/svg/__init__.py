"""drawsvg-based vector renderers.

Modules:
    drawing:  primitive helpers (rectangles, scale bars, annotation tracks)
    ideogram: whole-genome and zoom views
    tracks:   multi-track layouts (chromosome / acrocentric / region / gene / repeat)
    reads:    per-read fiber-seq vertical-bar layouts
    legend:   legend builder (4-col grid, dark / light themes)
    export:   SVG -> PNG via rsvg-convert
"""
