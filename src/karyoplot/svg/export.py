"""SVG -> PNG export via rsvg-convert.

Replaces the inline `_svg_to_png` helpers found in many scripts.

Populated in Phase 3.
"""

# Phase 3 will fill this with:
#   - svg_to_png(svg_path, png_path=None, scale=4) -> str
#       Calls rsvg-convert; warns (does not raise) if rsvg-convert is missing.
