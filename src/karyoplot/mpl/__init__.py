"""matplotlib-based publication plots.

Modules:
    style:       matplotlib font + theme defaults
    heatmap:     clustering + enrichment heatmaps
    comparison:  volcano, dotplot, lollipop
    statistics:  Fisher's exact, Mann-Whitney, fold change
    data_loader: sequence annotation TSV loaders
    legend:      matplotlib legend helpers
"""
