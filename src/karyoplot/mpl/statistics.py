"""Statistical comparisons used by mpl.comparison and mpl.heatmap.

Ported from feature_comparison_lib/statistics.py.

Populated in Phase 4.
"""

# Phase 4 will fill this with:
#   - fisher_exact_per_feature(counts) -> pd.DataFrame
#   - mann_whitney_per_feature(values) -> pd.DataFrame
#   - fold_change(a, b, log2=True) -> pd.Series
