"""Sequence annotation TSV loaders for matplotlib comparison plots.

Ported from feature_comparison_lib/data_loader.py.

Populated in Phase 4.
"""

# Phase 4 will fill this with:
#   - load_sequence_annotations(path) -> pd.DataFrame
#   - aggregate_per_sample(df, feature_groups) -> pd.DataFrame
#   - aggregate_per_read(df, feature_groups) -> pd.DataFrame
